version: "3.8"
services:
  clipcraft:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./videos:/app/videos
      - ./static:/app/static
    deploy:
      resources:
        limits:
          memory: 2G
